# mcbb

`mcbb` is a small pure-Python library that has some useful interfaces
for OpenMC simulations in blanket geometries. 

Linear geometries are explicitly given, but the base interface also works for stellarator geometry.

It can also generate weight windows using jax_sn. This is not a public repository yet.